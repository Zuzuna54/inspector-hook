# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
*.vsix

# IDE
.idea/
*.swp
*.swo
.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Test coverage
coverage/

# Temporary files
tmp/
temp/
*.tmp

# TypeScript incremental build cache (stale copies silently skip emit)
*.tsbuildinfo

# npm/yarn lockfiles - this is a pnpm workspace (pnpm-lock.yaml is the real one)
package-lock.json
yarn.lock

# graphify's build output: a 2.7MB graph.json plus a 2.8MB graph.html, both
# regenerable from source with `graphify update .` and both rewritten on every
# build. Committing them would put a machine-generated megabyte into every diff.
graphify-out/
